const Posts = username => {
  
  if (useranme) {
    setTimeout()
  }
  
  return `
    ${Nav()}
    <header>
      <h1>Posts</h1>
    </header>
    <main>
      <p>
        ${username ?  "loading posts for " + username : 'try clicking <a href="here/posts/greg'}
      </p>
    </main>
  `;
};
